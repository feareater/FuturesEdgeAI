node_modules/
.env
.claude/

# All local data — seed files, logs, backtest results, historical candles
# Re-generate seed data with: node server/data/seedFetch.js
/data/

# Databento historical data downloads (large zip files, not committed)
Historical_data/

# Scratch / temp files
*.log
*_tmp.json
*_tmp.js

# Runtime logs directory (server.log, pipeline_*.log)
logs/

# Binary installers (e.g. rtraderpro.msi in trading-knowledge/bootcamp)
*.msi
